# UI 规范

状态：占位文档。当前 AI Engineering OS 没有正式 UI；描述旧 Workflow/Evidence/Agent 界面的规范已删除（Git 历史即归档）。未来真正开发 AIOS UI 时，按 宪法第 6 条 重走 Prototype -> UI_SPEC -> 用户批准。

用户对前端实现的批准事实以本文件内的 approval: 元数据块为准（由 approval_record 写入，scope 精确匹配，不互相继承）。当前没有任何已批准的 scope。

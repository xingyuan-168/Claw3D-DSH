# 设计文档中心

状态：可执行设计基线（治理核心收敛后仅保留现行产物）
适用范围：未来控制台、桌面端或 Web/App 项目；当前 CLI 复用其中的信息架构。

## 文档地图

- [UI_SPEC.md](UI_SPEC.md)：页面行为、交互状态、可访问性和响应式规则。

## 设计门禁

见 [FRONTEND_GATE](../FRONTEND_GATE.md)：新页面 / 新交互流 / 重大 UI 重构必须先有交互式 HTML 原型（PROTOTYPE.html）与 UI_SPEC，用户批准后方可实现；改文案、修 CSS、修组件 Bug 豁免。

历史设计文档（USER_FLOW、UX_RESEARCH、WIREFRAME、COMPONENT_LIBRARY、DESIGN_TOKEN）已删除，需要时从 Git 历史恢复。

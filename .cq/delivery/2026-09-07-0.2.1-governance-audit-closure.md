# 0.2.1 治理审计与仓库收口交付摘要

- 日期：2026-09-07
- 分支：`codex/0.2.1-reliability-closure`（remote: origin，push 状态见文末）
- 需求基线：REQ-1.6.2 / 软件 0.2.1 / API·配置·文档·Profile 1.2 / SQLite 0008

## 任务

用户三项决定：①远程仓库固定 GitHub；②清理本地仓库（删除该删的、修复后推送、分支干净完结）；③三条要求写成机器可查硬规则。此前提：审计该治理仓库的优点、逻辑缺陷与规则重复，并制定修复方案。

## 变更内容（5 个 Conventional Commit）

1. `5c696af` feat(governance): 0.2.1 契约对齐 + ADR-0009 语义修复（40 文件）
   - **真 bug 修复**：ADR-0009 §3（G3/G4 证据绑定 = 目标 Commit 或祖先 + 内容复验）代码只实现一半——`evidence.py` checks 与 reviews 过滤仍残留"G3 精确相等"旧条件，导致集成合并后 E2E 无法到达 G4。已按 ADR 修正（`html-prototype-validator`/`ux-prototype` 保留精确相等）。
   - 修复 E2E 中遗留的 `raise AssertionError("DEBUG ...")` 调试代码；SQLite 0008 迁移、OCI-first 治理资产随 governance-repair 基线并入。
2. `281867e` chore(docs): document_version 基线升至 0.2.1（49 文件）；spec 一致性测试改为从 project.yaml 动态读取期望值。
3. `4808793` chore(governance): 清理遗留文档树
   - 删除 `docs/archive/`、`input/`（Git 历史是唯一存档）；AGENTS.md 不再赋予其参考地位；`documents.py` 移除 archive 元数据豁免（所有活跃 Markdown 必须有完整治理 metadata）。
   - 本地清理：0.1.0 构建产物、qa-021-* 孤儿克隆、9 个已合并 Worktree（含 Windows MAX_PATH 超长目录，用 `\\?\` 扩展路径删除）。
4. `261bf91` feat(governance): BR-070/071/072 硬规则
   - BR-070：GitHub 就绪是进入开发的唯一前置（Runtime 强制，所有 Workflow 入口共用）。
   - BR-071：每次迭代/修复必须推送 Commit（`GitEvidenceService` 真实 `ls-remote` 比对远端 SHA）+ 同步文档。
   - BR-072：工作区只留最新文档/源码；`repository_check` 新增 `UNDECLARED_LEGACY_DOCS` 与 `STALE_ARTIFACT` finding（旧构建产物版本 ≠ `RUNTIME_VERSIONS.software` 即阻塞），带负向测试。
5. `07eba63` docs(governance): ADR-0010 + 25+ 处版本漂移修复（15 个活跃文档）
   - ADR-0010：`RUNTIME_VERSIONS` 为唯一版本事实源（PROJECT_MASTER 为权威散文矩阵位）、强制力分层声明（Hook 是防御纵深非安全边界）、workflow_phase/run_status/Host Operation 三词汇表对照。
   - 修复 SQLite 0007→0008、v0.2.0→v0.2.1、迁移链、G3 语义（对齐 ADR-0009）、Podman-first 默认注记；历史 ADR 与 CHANGELOG 历史条目冻结不动。
   - 防复发：`test_spec_consistency` 把 8 个文档的版本锚点行钉到 `RUNTIME_VERSIONS`，未来漂移在 commit 前失败。
6. `eafeb6f` feat(doctor): `plugin-hooks` 防御纵深探测（非阻塞；checkout 内验证 Hook 清单脚本落盘，仓库外交由 Codex 宿主注册）。

## 验证

- 全量 pytest：377 passed, 1 skipped（真实 Podman 需显式启用）——含新增 6 个测试。
- `git diff --check`、Secret Scan（0 findings）、check-docs 全部通过。
- 每个提交独立逻辑变更 + Conventional Commit，工作树干净。

## 已知问题与后续

- **push pending**：会话审批策略拦截网络类命令（git push），5 个提交留在本地，需手动 `git push origin HEAD`。推送后本分支可合入 main 完结。
- `governance-repair-20260907` 分支（0474eca）保留远端作为已批准基线记录，其内容已由 `5c696af` 超集覆盖。
- P1 后续建议（未列入本轮）：Reviewer 独立性在 review 代码注释中显式化；`MIGRATION_SPEC` 0008 节与迁移器错误码的测试级联动可再加密度。
